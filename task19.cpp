19) #include<iostream>
#include<cmath>
using namespace std;
int main() {
	double a, b;
	cin >> a >> b;
	int n, m;
	n = (0, 3 * a) + (0, 4 * b);
	m = 3 * ((2 * b) + (1.8 * a));
	cout << n << " " << m;
	return 0;
}
